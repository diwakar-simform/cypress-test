name: Cypress-e2e
on: push
jobs:
    cypress-run:
        runs-on: Windows (x86) 10 2004/20H2/21H1/21H2
        steps:
            - name: checkout
              uses: actions/checkout@v4
            - name: install cypress
              uses: npm install
            - name: Cypress run
              uses: cypress-io/github-action@v6

